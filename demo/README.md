# Synthetic demo dataset

Every value in this directory is fictional and intended only for testing Cleanroom.
Copy one or more `.txt` files into `dirty/`, then run `cleanroom scan`.

The examples exercise deterministic and contextual categories. Model findings can
vary; use `cleanroom evaluate` for repeatable expected-span evaluation.

