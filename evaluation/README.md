# Synthetic evaluation dataset

All values here are fabricated. `cases/` contains local text fixtures and `expected/`
contains exact spans marked required or optional. The PDF case is generated in a
controlled temporary directory from synthetic content and then removed. Text and PDF
metrics are reported separately. Results are written outside this directory to ignored
`evaluation-results/`.
